#include "holberton.h"


char**divider (char *string, char* delimiter)
{
    int i;
    int size;
    char **token = malloc(sizeof(char *));
    if (token == NULL)
    {
        return NULL;
    }


    return NULL;
}